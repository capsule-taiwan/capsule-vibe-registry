# vibe-registry 設定檔範本
# 正常流程：安裝後第一次開 session，Claude 會依 hook 注入的指示引導使用者
# 完成設定並自動產生 ~/.claude/vibe-registry.env（本檔只是格式參考，
# 也可由 IT 複製為本目錄的 config.env 手動放置，優先於 home 設定）。
#
# REGISTRY_TOKEN: 登記表閘道 token（只對 vibe-registry 函式有效，不是資料庫憑證）
# OWNER_EMAIL: 使用者的公司 email（登記時的負責人；留空則退回 git config user.email）
# REGISTRY_URL: 選填。腳本已內建預設值，只有換 Supabase 專案時才需要填來覆蓋

REGISTRY_TOKEN=""
OWNER_EMAIL=""
# REGISTRY_URL=""
